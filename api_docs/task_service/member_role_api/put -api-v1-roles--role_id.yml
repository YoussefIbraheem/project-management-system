info:
  name: put /api/v1/roles/:role_id
  type: http
  seq: 5
  tags:
    - member_role_api

http:
  method: PUT
  url: "{{url}}/roles/:role_id"
  params:
    - name: role_id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Successful Response
    request:
      url: "{{url}}/roles/:role_id"
      method: PUT
      params:
        - name: role_id
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
