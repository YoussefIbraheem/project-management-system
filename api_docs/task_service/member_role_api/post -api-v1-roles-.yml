info:
  name: post /api/v1/roles/
  type: http
  seq: 2
  tags:
    - member_role_api

http:
  method: POST
  url: "{{url}}/roles/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Successful Response
    request:
      url: "{{url}}/roles/"
      method: POST
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
