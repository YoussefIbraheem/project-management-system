info:
  name: post /api/v1/boards/:board_id/columns
  type: http
  seq: 2
  tags:
    - board_column_api

http:
  method: POST
  url: "{{url}}/boards/:board_id/columns"
  params:
    - name: board_id
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "label": "Progressing",
        "slug": "progressing",
        "status_group": "in_progress"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Successful Response
    request:
      url: "{{baseUrl}}/api/v1/boards/:board_id/columns"
      method: POST
      params:
        - name: board_id
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
