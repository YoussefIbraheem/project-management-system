info:
  name: post /api/v1/projects/
  type: http
  seq: 2
  tags:
    - project_api

http:
  method: POST
  url: "{{url}}/projects/"
  body:
    type: json
    data: |-
      {
        "description": "",
        "name": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Successful Response
    request:
      url: "{{url}}/projects/"
      method: POST
      body:
        type: json
        data: |-
          {
            "description": "",
            "name": ""
          }
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
