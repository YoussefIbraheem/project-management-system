info:
  name: put /api/v1/projects/:project_id/members/:user_id
  type: http
  seq: 5
  tags:
    - project_member_api

http:
  method: PUT
  url: "{{url}}/projects/:project_id/members/:user_id"
  params:
    - name: project_id
      value: ""
      type: path
    - name: user_id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Successful Response
    request:
      url: "{{baseUrl}}/api/v1/projects/:project_id/members/:user_id"
      method: PUT
      params:
        - name: project_id
          value: ""
          type: path
        - name: user_id
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
