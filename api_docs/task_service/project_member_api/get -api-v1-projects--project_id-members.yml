info:
  name: get /api/v1/projects/:project_id/members
  type: http
  seq: 1
  tags:
    - project_member_api

http:
  method: GET
  url: "{{url}}/projects/:project_id/members"
  params:
    - name: project_id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Successful Response
    request:
      url: "{{baseUrl}}/api/v1/projects/:project_id/members"
      method: GET
      params:
        - name: project_id
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
