info:
  name: post /api/v1/boards/
  type: http
  seq: 2
  tags:
    - board_api

http:
  method: POST
  url: "{{url}}/boards/"
  body:
    type: json
    data: |-
      {
        "default_columns": false,
        "description": "",
        "name": "",
        "project_id": 0
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Successful Response
    request:
      url: "{{url}}/boards/"
      method: POST
      body:
        type: json
        data: |-
          {
            "default_columns": false,
            "description": "",
            "name": "",
            "project_id": 0
          }
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
