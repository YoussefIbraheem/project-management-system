info:
  name: get /api/v1/boards/:board_id/columns
  type: http
  seq: 6
  tags:
    - board_api

http:
  method: GET
  url: "{{url}}/api/v1/boards/:board_id/columns"
  params:
    - name: board_id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Successful Response
    request:
      url: "{{url}}/api/v1/boards/:board_id/columns"
      method: GET
      params:
        - name: board_id
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
