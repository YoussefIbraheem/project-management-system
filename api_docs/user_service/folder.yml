info:
  name: user_service
  type: folder
  seq: 2

request:
  headers:
    - name: Accept
      value: application/json
  auth:
    type: bearer
    token: "{{accessToken}}"
  variables:
    - name: url
      value: "{{baseUrl}}8000/api/v1"
    - name: ""
      value: ""
      disabled: true
  actions:
    - type: set-variable
      phase: after-response
      selector:
        expression: ""
        method: jsonq
      variable:
        name: ""
        scope: runtime
      disabled: true
  scripts:
    - type: after-response
      code: |-
        // let res_status =res.status
        // console.log(res_status == 401)
        // if (res_status == 401){
        //   const ref_res = await bru.sendRequest({
        //   method: 'POST',
        //   url: "http://localhost:8000/api/v1/login/refresh/",
        //   headers: {
        //     'refresh': `Bearer ${bru.getVar("refreshToken")}`,
        //     'Content-Type': 'application/json'
        //   }
        //   })
        //   let accessToken = ref_res.body.access
        //   let refreshToken = ref_res.body.refresh
        //   bru.setVar("accessToken",accessToken)
        //   bru.setVar("refreshToken", refreshToken)
        //   console.log(ref_res.body)
        // }
