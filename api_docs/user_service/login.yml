info:
  name: login
  type: http
  seq: 5

http:
  method: POST
  url: "{{url}}/login/"
  headers:
    - name: Accept
      value: application/json
  body:
    type: multipart-form
    data:
      - name: email
        type: text
        value: admin@admin.com
      - name: password
        type: text
        value: password@123
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        let accessToken = res.body["tokens"]["access"]
        let refreshToken = res.body["tokens"]["refresh"]
        bru.setVar("accessToken",accessToken)
        bru.setVar("refreshToken", refreshToken)

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
