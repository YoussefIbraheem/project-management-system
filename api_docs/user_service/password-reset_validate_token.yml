info:
  name: password-reset_validate_token
  type: http
  seq: 2
  tags:
    - password-reset

http:
  method: POST
  url: "{{url}}/password-reset/validate_token/"
  body:
    type: json
    data: |-
      {
        "token": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: An Api View which provides a method to verify that a token is valid
